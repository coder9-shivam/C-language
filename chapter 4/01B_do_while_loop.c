#include <stdio.h>

int main()
{
    int i = 0;

    do
    {
        printf("the value of i is %d\n", i);      // ( do while loop executes at least for one time)
        i++;
    } while (i<=10);

    return 0;
}