#include <stdio.h>

int main()
{
    int a = 4;
    // int b = 8.5; // not recommended because 8.5 is not an integer
    float b = 8.55;
    char c = 'u';
    int d = 45;
    int e = 45 + 4;
    // int f = 8.4;// not recommended because 8.4 is not an integer
    printf("The value of a is %d \n", a);
    printf("The value of b is %f \n", b);
    printf("The value of b is %c \n", c);
    printf("sum of a and d is %d \n", a + d);
    printf("sum of a and d is %d \n", e);
    return 0;
}



// try it yourself ---> create a program to add two number in C. 